
var ctrls = angular.module('controllers', []);

ctrls.controller('authController', [
	'$scope', 'socket', '$cookieStore', '$rootScope',
	function ($scope, socket, $cookieStore, $rootScope) {
		
	}
]);